.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install minki

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv minki
    $ pip install minki
